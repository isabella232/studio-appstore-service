@model AppStoreIntegrationService.Model.ModalMessage;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<div class="modal fade" id="confirmationModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">@Model.Title</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                @Model.Message
            </div>
            <div class="modal-footer">
                @if (Model.ModalType == AppStoreIntegrationService.Model.ModalType.SuccessMessage)
                {
                    if (!string.IsNullOrEmpty(Model.RequestPage) && Model.RequestPage.ToLower().Equals("add"))
                    {
                        <button type="button" onclick="javascript: RedirectToList()" class="btn btn-secondary add-pluginBtn" data-dismiss="modal">Ok</button>
                    }
                    else
                    {
                        <button type="button" class="btn btn-primary add-pluginBtn" style="margin-bottom:0px" data-dismiss="modal">Continue</button>
                        <button type="button" onclick="javascript: RedirectToList()" class="btn btn-secondary" data-dismiss="modal">Redirect</button>
                    }
                }
                @if (Model.ModalType == AppStoreIntegrationService.Model.ModalType.WarningMessage)
                {
                    <button type="button" class="btn btn-primary add-pluginBtn" style="margin-bottom:0px" data-dismiss="modal">Ok</button>
                }

            </div>
        </div>
    </div>
</div>

